// Created a function that selects a pokemon (selectPokemon) which gets triggered when a user clicks on a pokemon.
function selectPokemon(pokemonName) {
    // Sets a cookie for the "chosenPokemonName".
    Cookies.set("chosenPokemonName", pokemonName);
}
